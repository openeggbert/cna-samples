#pragma once

#include "Control.hpp"

namespace DynamicMenu::Controls {

// Defines an image control. Control already has all the properties needed to
// show an image; this class exists only to make the purpose of a control
// explicit. Port of Controls/Image.cs.
class Image : public Control {
    // Nothing at this level
};

} // namespace DynamicMenu::Controls
