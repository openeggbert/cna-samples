# NEXT.md — CNA Samples Handoff

## 1. Project Summary

**What:** C++ ports of the official Microsoft XNA Game Studio 4.0 sample collection,
running on CNA — a C++ reimplementation of the XNA 4.0 API built on SDL3 + Vulkan backend.

**Main goal:** Port all ~83 applicable XNA 4.0 desktop samples to CNA C++, preserving
the original class hierarchy and naming (`Microsoft::Xna::Framework::*`), serving as
living integration tests for the CNA framework and a migration reference.

**Current phase:** Phase 1 (Foundation — 10 of 12) and Phase 2 (2D Games — 17 samples)
are fully complete. Phase 5 (Audio) has started — Audio3D (#059) is done.
CameraShake (#030, Phase 2) is also done.

**Key architectural decisions:**
- One executable per sample; no shared sample library.
- All sample code is header-only (except `Program.cpp`) to avoid the need for
  separate translation units.
- Assets: `Load<Texture2D>()` → PNG; `Load<SpriteFont>()` → `.font.json` + PNG atlas
  (generated by `tools/make_font.py`); `Load<SoundEffect>()` → WAV;
  `Load<Song>()` → MP3/OGG/WAV/FLAC/WMA; `Load<Model>()` → `.model.json` format.
- `CONTENT_DIR` argument to `cna_add_sample()` is mandatory for any sample that
  loads assets at runtime.

---

## 2. Current Status

### Build
All enabled samples compile and run cleanly on the Vulkan backend.

### Enabled samples and their status
| #   | Sample              | Status  | Notes |
|-----|---------------------|---------|-------|
| 001 | PrimitivesSample    | ✅ runs | 2D DrawUserPrimitives |
| 002 | Primitives3D        | ✅ runs | DiffuseColor B-key and wireframe Y-key visually broken (EasyGL bugs) |
| 003 | TexturesAndColors   | ✅ runs | |
| 006 | SpriteEffects       | ✅ runs | |
| 007 | SpriteSheet         | ✅ runs | |
| 008 | ShapeRendering      | ✅ runs | |
| 009 | InputReporter       | ✅ runs | gamepad display, 6 SpriteFont sizes |
| 010 | InputSequence       | ✅ runs | move detection, SpriteFont labels |
| 011 | SafeArea            | ✅ runs | corner labels, safe-area overlay |
| 012 | GeneratedGeometry   | ✅ runs | |
| 013 | Platformer          | ✅ runs | full game: 3 levels, player, enemies, gems, HUD |
| 015 | TicTacToe           | ✅ runs | |
| 016 | Bounce              | ✅ runs | |
| 017 | CollisionSample     | ✅ runs | |
| 018 | PerPixelCollision   | ✅ runs | |
| 019 | RectangleCollision  | ✅ runs | |
| 020 | TransformedCollision| ✅ runs | |
| 021 | PathDrawing         | ✅ runs | |
| 022 | Pathfinding         | ✅ runs | |
| 023 | WaypointSample      | ✅ runs | |
| 024 | FlockingSample      | ✅ runs | |
| 025 | ChaseAndEvade       | ✅ runs | |
| 026 | AimingSample        | ✅ runs | |
| 027 | FuzzyLogic          | ✅ runs | |
| 029 | ParticleSample      | ✅ runs | |
| 030 | CameraShake         | ✅ runs | 3D scene renders as thin line (CNA Vulkan near-plane issue with large geometry) |
| 059 | Audio3D             | ✅ runs | 3D positional audio; cat/dog sprites; alpha-test quads |

### Deferred (not built)
| #   | Sample          | Blocker |
|-----|-----------------|---------|
| 005 | ReachGraphicsDemo | Model + SkinnedAnimation + SpriteFont menus |
| 014 | Spacewar        | Model + custom shaders + XACT |
| 028 | ColorReplacement | Model + custom Effect |
| 031 | BloomSample     | custom Effect (3 shaders) + RenderTarget2D + Model |

### What does not work / known CNA bugs
- `BasicEffect.DiffuseColor` ignored for `VertexPositionColor` meshes (EasyGL bug in `../cna`).
- Wireframe mode (`FillMode::WireFrame`) silently ignored on OpenGL ES (EasyGL limitation).
- 3D scene in CameraShake renders as a thin white line — camera is inside the scaled ground's XZ
  footprint; Vulkan near-plane clipping with large geometry. Documented in CameraShake/missing.md.
- Phase 3+ samples with custom HLSL shaders require manual GLSL translation (item 11 in DEFERRED.md).

---

## 3. Recent Changes

- **samples/Audio3D/** (new) — Port of XNA Audio3D (#059): `IAudioEmitter`, `SpriteEntity`,
  `Cat`, `Dog`, `AudioManager` (GameComponent), `QuadDrawer` (AlphaTestEffect billboard renderer),
  `Audio3DGame`. 3D positional audio with cat circling, dog barking. Arrow keys move camera.
- **samples/CameraShake/** (earlier session) — Port of XNA CameraShake (#030): camera shake
  effect with A/X keys, tank + ground 3D models (thin-line visual issue documented).
- **PLAN.md** — Audio3D (#059) marked ✅ Done.

---

## 4. Current Blocker / Main Problem

No hard blocker — all enabled samples build and run.

The remaining **Phase 3 (3D Graphics)** samples almost all require custom HLSL shaders
(translated to GLSL `.shader.json`) — significant effort per sample (see DEFERRED.md #11).

The remaining **Phase 5 (Audio)** sample is SoundAndMusic (#060), which has a complex
UI with sliders/buttons — manageable but more work than Audio3D.

**Best next candidate:** #060 SoundAndMusic — pure audio + 2D UI, no 3D, no custom shaders.

---

## 5. Known Bugs and Limitations

| Label | Description |
|-------|-------------|
| confirmed bug | **Primitives3D: DiffuseColor ignored.** `BasicEffect.setDiffuseColorProperty()` has no effect for `VertexPositionColor` geometry. EasyGL selects shader by vertex stride. Fix in `../cna`. |
| confirmed bug | **Primitives3D: wireframe silent.** `FillMode::WireFrame` not supported on OpenGL ES. |
| confirmed bug | **CameraShake: 3D scene thin line.** Camera at (1000,1000,1000) is inside scaled ground's XZ footprint (±6553 units). Vulkan near-plane clipping bug with extreme clip-space overflow. |

---

## 6. Architecture Notes

### Sample directory layout
```
samples/SampleName/
  src/
    Program.cpp          # int main() { Game g; g.Run(); return 0; }
    GameClass.hpp        # all logic inline (no .cpp unless circular deps)
  Content/               # copied next to binary at build time via CONTENT_DIR
  CMakeLists.txt         # cna_add_sample(name SOURCES src/Program.cpp CONTENT_DIR ...)
  missing.md             # required: differences vs XNA original
  SampleName.htm         # required: original MS documentation, verbatim copy
```

### CNA API rules — must not break
- All XNA properties: `getXxxProperty()` / `setXxxProperty()` — never raw member access.
- Every `Game` subclass must implement:
  ```cpp
  const std::string& GetTypeName() const override {
      static const std::string name = "ClassName";
      return name;
  }
  ```
- `Content.Load<T>()` returns **T by value** — store in `std::optional<T>` field.
- `SpriteBatch::Draw` takes `const Texture2D&` — use `*optionalTex`.
- Virtual override signatures: `void Update(GameTime& gameTime)` and
  `void Draw(const GameTime& gameTime)` — note the const on Draw.
- No `operator+=` for `Vector2` or `TimeSpan` — write `v = v + expr`.
- `SoundEffect` has no default constructor — use `emplace()` not `operator[]` in maps.
- `SoundEffectInstance` is move-only — wrap in `std::unique_ptr<SoundEffectInstance>`.
- `SoundEffect::CreateInstance()` returns by value — move into unique_ptr:
  `auto inst = sf.CreateInstance(); ptr = std::make_unique<SoundEffectInstance>(std::move(inst));`
- `AudioListener`/`AudioEmitter` use property-style setters:
  `listener.setPositionProperty(pos)` — not `listener.Position = pos`.
- `GameComponent` subclasses: call `GameComponent::Initialize()` and `GameComponent::Update()` at end of overrides.
- `getComponentsProperty().Add(ptr)` to register components with Game.

### SpriteFont generation
```bash
python3 tools/make_font.py /path/to/Font.ttf <size_px> samples/X/Content/FontName
```

### Sound assets
- `SoundEffect` loads `.wav` only.
- `Song` (music) loads: `.mp3 .ogg .wav .flac .opus .aac .wma`

### Help overlay
```bash
python3 tools/gen_help_png.py samples/SampleName/SampleName.htm samples/SampleName/Content/help.png
```

---

## 7. Useful Commands

```bash
# Build all enabled samples
cmake --build cmake-build-debug

# Run Audio3D (arrow keys to move camera, hear 3D cat/dog sounds)
./cmake-build-debug/samples/Audio3D/Audio3D_cna_samples

# Run CameraShake (A = short shake, X = long shake)
./cmake-build-debug/samples/CameraShake/CameraShake_cna_samples

# Generate SpriteFont
python3 tools/make_font.py /usr/share/fonts/truetype/dejavu/DejaVuSans.ttf 19 \
    samples/MySample/Content/HudFont
```

---

## 8. Next Smallest Tasks

1. **Port #060 SoundAndMusic** — audio controls (volume/pitch/pan sliders), SoundEffect + Song
   - Source: `/rv/tmp/XNAGameStudio/Samples/SoundAndMusic_4_0/`
   - Complex UI with Button helpers, but pure 2D, no 3D or shaders.

2. **Port Phase 3 samples with custom shaders** — requires translating HLSL `.fx` to GLSL.
   Simplest candidates: PerPixelLighting, VertexLighting, RimLighting (single model + one shader).

3. **Fix Primitives3D DiffuseColor bug in CNA** (visible regression, affects B-key)
   - File: `../cna/src/CNA/Internal/Backends/EasyGL/EasyGLGraphicsBackend.cpp`

---

## 9. Do Not Do

- **Do not start Phase 3 samples without translating their HLSL shaders first.**
- **Do not add a shared `samples/common/` library** — each sample must be self-contained.
- **Do not re-generate existing font atlases** unless there is a confirmed rendering bug.
- **Do not mass-update PLAN.md statuses** in a single commit.
